query GetAdvertiser($advertiserId: String) {
  advertiser(advertiserId: $advertiserId) {
    id
    name
    description
    logo {
      url
    }
    statusId
    commission {
      id
      commission
      dayToValidate
      dayToPayout
      url
    }
  }
}
