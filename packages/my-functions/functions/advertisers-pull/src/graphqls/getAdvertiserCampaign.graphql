query GetAdvertiserCampaign(
  $providerId: AffiliateProviderEnum!
  $providerReferenceId: String!
) {
  advertiserCampaignByProvider(
    providerId: $providerId
    providerReferenceId: $providerReferenceId
  ) {
    id
  }
}
