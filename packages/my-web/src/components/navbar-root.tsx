"use client";
import { SearchRoot } from "@tekminewe/mint-ui/search-input";

export const NavbarRoot = ({ children }: { children: React.ReactNode }) => {
  return <SearchRoot>{children}</SearchRoot>;
};
